# Minimal requirements for MCP servers (Docker-compatible, ARM-friendly)
# Core MCP infrastructure only

# Core dependencies
pydantic>=2.0.0
aiohttp>=3.9.0
httpx>=0.27.0

# Numerical computing (required for most servers)
numpy>=1.24.0
scipy>=1.11.0

# Deep Learning (required for AI features)
torch>=2.0.0

# Optional: Skip problematic packages for ARM
# QuantLib-Python - Not available for ARM64
# TA-Lib - Requires compilation

# Logging
python-dotenv>=1.0.0